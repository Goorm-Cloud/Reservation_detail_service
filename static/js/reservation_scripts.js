document.addEventListener("DOMContentLoaded", function () {
    const reservationForm = document.getElementById("reservationForm");

    if (reservationForm) {
        // 폼 제출 이벤트 리스너 제거 - 서버 측 리다이렉트 사용
        // 기본 HTML 폼 제출 동작 사용
    }
});